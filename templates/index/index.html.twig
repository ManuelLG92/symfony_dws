
{% extends 'base.html.twig' %}

{% block title %}Buy-m3!{% endblock %}

{% block body %}
    {% set imgHeight = "150" %}
    {% set imgWidth = "100" %}


<div class="container rounded contenedor p-3">

    <input type="hidden" class="carro" name="carro" value="{{ carro }}">
    <h2 class="text-center mb-5">Bienvenido a Buy-m3</h2>

    <hr>
    {{ include('articulo/_seccion_articulo.html.twig', {'seccion': electronica,
        'imgHeight' : imgHeight,
        'imgWidth' : imgWidth,
        titulo : 'Electronica'}) }}
    {% if electronica|length > 1 %}
    <div class="text-center mt-3 mb-3">
        <h5>Si quieres ver todos los productos de esta seccion
            <a href="{{ path('articulos_seccion', {'seccion' : idElectronica}) }}"
               target="_blank">
                pincha aqui.
            </a>
        </h5>
    </div>
    {% endif %}
    <hr>

    <br>
    {{ include('articulo/_seccion_articulo.html.twig', {'seccion': educacion,
        'imgHeight' : imgHeight,
        'imgWidth' : imgWidth,
        titulo : 'Educacion'}) }}
    {% if educacion|length > 1 %}
    <div class="text-center mt-3 mb-3">
        <h5>Si quieres ver todos los productos de esta seccion
            <a href="{{ path('articulos_seccion', {'seccion' : idEducacion}) }}"
               target="_blank">
                pincha aqui.
            </a>
        </h5>
    </div>
    {% endif %}
    <hr>

    {{ include('articulo/_seccion_articulo.html.twig', {'seccion': alimentacion,
        'imgHeight' : imgHeight,
        'imgWidth' : imgWidth,
        titulo : 'Alimentacion'}) }}
    {% if alimentacion|length > 1 %}
    <div class="text-center mt-3 mb-5">
        <h5>Si quieres ver todos los productos de esta seccion
            <a href="{{ path('articulos_seccion', {'seccion' : idAlimentacion}) }}"
               target="_blank">
                pincha aqui.
            </a>
        </h5>
    </div>
    {% endif %}
    <hr>
    <br>
    {{ include('articulo/_seccion_articulo.html.twig', {'seccion': hogar,
        'imgHeight' : imgHeight,
        'imgWidth' : imgWidth,
        titulo : 'Hogar'}) }}
    {% if hogar|length > 1 %}
    <div class="text-center mt-3 mb-3">
        <h5>Si quieres ver todos los productos de esta seccion
            <a href="{{ path('articulos_seccion', {'seccion' : idHogar}) }}"
               target="_blank">
                pincha aqui.
            </a>
        </h5>
    </div>
    {% endif %}
    <hr>

    <br>
    {{ include('articulo/_seccion_articulo.html.twig', {'seccion': muebles,
        'imgHeight' : imgHeight,
        'imgWidth' : imgWidth,
        titulo : 'Muebles'}) }}
    {% if muebles|length > 1 %}
    <div class="text-center mt-3 ">
        <h5 class="mb-3">Si quieres ver todos los productos de esta seccion
            <a href="{{ path('articulos_seccion', {'seccion' : idMuebles}) }}"
               target="_blank">
                pincha aqui.
            </a>
        </h5>
    </div>
    {% endif %}
    <hr>

    <br>
    {{ include('articulo/_seccion_articulo.html.twig', {'seccion': motor,
        'imgHeight' : imgHeight,
        'imgWidth' : imgWidth,
        titulo : 'Motor'}) }}
    {% if motor|length > 1 %}
        <div class="text-center mt-3 ">
            <h5 class="mb-3">Si quieres ver todos los productos de esta seccion
                <a href="{{ path('articulos_seccion', {'seccion' : idMotor}) }}"
                   target="_blank">
                    pincha aqui.
                </a>
            </h5>
        </div>
    {% endif %}
    <hr>

    <br>
    {{ include('articulo/_seccion_articulo.html.twig', {'seccion': otros,
        'imgHeight' : imgHeight,
        'imgWidth' : imgWidth,
        titulo : 'Otros'}) }}
    {% if otros|length > 1 %}
    <div class="text-center mt-3 mb-5">
        <h5 class="pb-3">Si quieres ver todos los productos de esta seccion
            <a href="{{ path('articulos_seccion', {'seccion' : idOtros}) }}"
               target="_blank">
                pincha aqui.
            </a>
        </h5>
    </div>
    {% endif %}

</div>
    <script src="{{ asset('js/carroCompra.js') }}" type="text/javascript">

    </script>
{% endblock %}
